<link rel="stylesheet" href="/css/group-details.css" />

<div class="container">
  <h1>Group Events</h1>
  <div>
    <a href="javascript:history.back();">Back to Group Page</a>

    {{#each events}}
      <div class="event">
        <h2>{{title}}</h2>
        {{#if image}}
          <img src="data:image/png;base64,{{image}}" alt="Event Image" />
        {{/if}}

       <form
  action="/groups/{{../_id}}/eventEdit/{{_id}}"
  method="POST"
  id="event-edit-form"
>
          <div class="form-group">
            <label for="title">Title</label>
            <input
              type="text"
              name="title"
              id="title"
              class="form-control"
              value="{{title}}"
            />
          </div>
          <div class="form-group">
            <label for="eventDate">Event Date</label>
            <input
              type="datetime-local"
              name="eventDate"
              id="eventDate"
              class="form-control"
              value="{{eventDate}}"
            />
          </div>
          <div class="form-group">
            <label for="description">Description</label>
            <textarea
              name="description"
              id="description"
              class="form-control"
            >{{description}}</textarea>
          </div>
          <div class="form-group">
          </div>
          <input type="submit" value="Save" class="btn btn-primary" />
        </form>
      </div>
    {{/each}}
  </div>
</div>
<div id="error"></div>
<script src="/public/js/groupEdit.js"></script>

{{message}}
